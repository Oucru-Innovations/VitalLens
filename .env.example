# =============================================================
# VitalLens runtime config - COPY file nay sang `.env` va dien gia tri that.
# KHONG commit file `.env` (chua secret).
# =============================================================

# --- API upload (bat buoc neu muon day PDF+CSV len backend) ---
API_UPLOAD_URL=https://your-api.example.org/upload
API_BEARER_TOKEN=replace_me_with_real_token

# --- SFTP (co default trong code, chi dien neu muon override) ---
# SFTP_HOST=datastore.oucru.org
# SFTP_PORT=22
# SFTP_DEMO_MODE=false
# SFTP_PATH=/EI_SHARE/.received/13NV/PROCESSING
