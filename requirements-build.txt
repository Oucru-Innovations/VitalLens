# =====================================================================
# VitalLens - build toolchain
#
# Chỉ cần trên máy build EXE. Tách khỏi requirements.txt để môi trường
# chạy app không kéo theo PyInstaller/Nuitka.
# Rà phiên bản lần cuối: 2026-08-18, Python 3.12.13 / Windows.
#
#   pip install -r requirements-build.txt
# (dòng dưới tự kéo luôn toàn bộ runtime deps)
# =====================================================================

-r requirements.txt

pyinstaller==6.21.0
pyinstaller-hooks-contrib==2026.6

# Nuitka: nhánh build onefile (build_nuitka.bat) — 1 file .exe duy nhất.
# Cần thêm MSVC (Visual Studio Build Tools) hoặc clang trên máy build;
# Nuitka tự tải ccache/dependency-walker khi có --assume-yes-for-downloads.
nuitka==4.1.3
