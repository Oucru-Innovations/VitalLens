# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Virtual envs
.venv/
venv/
env/

# Secrets / local config
.env
.env.*
!.env.example
# The `env` file (no dot) at repo root — VitalLens also loads this
# as a fallback, so it must be ignored to avoid leaking tokens.
/env

# PyInstaller / Nuitka build artifacts
build/
dist/
dist_nuitka/
*.build/
*.dist/
*.onefile-build/
*.manifest
*.spec.bak
nuitka-crash-report.xml

# IDE / editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# VitalLens runtime output (do NOT commit exported data)
output_pdf/
output_xray/
KetQua_*.xlsx
OCR_*.xlsx

# Per-user runtime state (last-used upload workspace folder)
.vitallens_workspace.json

# Logs
*.log
